<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Warna background CardView -->
    <solid android:color="@color/colorSurface" />

    <!-- Sisi dengan warna tertentu -->
    <stroke
        android:color="@color/colorPrimary2"
    android:width="2dp" />

    <!-- Radius sudut CardView -->
    <corners android:radius="4dp" />

    <padding
        android:left="4dp"
        android:right="4dp"
        android:top="4dp"
        android:bottom="4dp" />
</shape>
